# datasource config
spring.datasource.url=jdbc:postgresql://localhost:5432/ninegolddb
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.showSql=true

# you can send forgot password for single user with limited times
forgotPassword.maxTimes = 5
# will expire after 3 days, unit is milliseconds 60*60*1000*24*3
forgotPassword.expirationTimeInMillis=259200000

# mail config
spring.mail.host=localhost
spring.mail.port=1024
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.starttls.required=false

spring.velocity.resourceLoaderPath=classpath:/templates/
spring.velocity.prefix=
spring.velocity.suffix=.vm
spring.velocity.cache=false
spring.velocity.check-template-location=true
spring.velocity.content-type=text/html
spring.velocity.charset=UTF-8
spring.velocity.properties.input.encoding=UTF-8
spring.velocity.properties.output.encoding=UTF-8

# from address
mail.from=test@test.com

# stripe
stripe.apiKey=sk_test_STn9YrQp7OzbDZk6KK5NDhMt
stripe.plan.name=Basic Monthly
stripe.plan.type=basic-monthly
stripe.plan.interval=month
stripe.plan.currency=usd
stripe.plan.amount=10

payment.failure.terminate.account=true

log4j.category.org.springframework.beans.factory=DEBUG
